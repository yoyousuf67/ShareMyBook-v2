  <h1>{{title}}</h1>
  <p>Welcome to {{title}}</p>
  {{# if condition}}
    condition is met
  {{else}}
    condition is not met
  {{/if}}

  {{# each anyArray as |val key|}}
    {{key}}:{{val}}
  {{/each}}


  <form class="" action="/test/submit" method="post">
    <input type="text" name="id" value="">
    <button type="submit">Submit</button>
  </form>
